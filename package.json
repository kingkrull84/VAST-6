{
  "name": "vast6",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "wasm:build": "npx wasm-pack build --target web --out-dir pkg",
    "dev": "npm run wasm:build && vite",
    "build": "npm run wasm:build && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "vite-plugin-top-level-await": "^1.4.0",
    "vite-plugin-wasm": "^3.3.0"
  },
  "dependencies": {
    "three": "^0.186.0"
  }
}
